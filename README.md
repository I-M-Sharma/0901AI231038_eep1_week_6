# Week 6 Text Analyzer Project

This is an interactive command-line tool built in Python. It allows users to analyze multiple text files inside a specific folder by providing statistics, searching for keywords, and finding the most frequent words.

## Usage Instructions

To run this script, open your terminal and use the following command to start the interactive prompt:

```bash
python analyze.py ./test_folder
